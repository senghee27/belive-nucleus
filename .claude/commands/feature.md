# /feature

Scaffold a new feature branch and file structure.

Steps:
1. Ask: what is the feature name?
2. Create git branch: feature/[name] from dev
3. Identify which files need to be created or modified
4. List them with their purpose
5. Ask which agent should build each part
6. Start with the database migration if schema changes needed
